# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# Vim swap files.
.sw?
.*.sw?

# The executable. Sometimes I'm in this directory when doing development.
/jt

# antlr generated files that shouldn't be checked in.
/parser/

# Keep downloaded dependencies from getting checked in.
/vendor/

/gobfile
/gobfile.go
